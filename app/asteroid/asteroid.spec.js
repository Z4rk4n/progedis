'use strict';

describe('myApp.asteroid module', function() {

  beforeEach(module('myApp.asteroid'));

  describe('asteroid controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('asteroidCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});